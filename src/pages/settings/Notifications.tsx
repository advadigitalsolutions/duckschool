import { ComingSoon } from '@/components/ComingSoon';

export default function Notifications() {
  return (
    <ComingSoon 
      feature="Notification Settings"
      description="Control email, SMS, and push notifications for assignments, grades, and reminders."
    />
  );
}